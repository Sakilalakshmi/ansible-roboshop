- name: creating MongoDB component
  hosts: mongodb
  become: yes
  tasks:
  - name: copying mongo repo to yum.repos.d
    ansible.builtin.copy:
      src: mongo.repo
      dest: /etc/yum.repos.d/mongo.repo
  - name: Install mongodb
    ansible.builtin.yum:
      name: mongodb-org
      state: installed